<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SmsRecord">
    <sql id="sms_record_K">
        <trim suffix="" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="keyVal != null">keyVal,</if>
            <if test="createTime != null">createTime,</if>
            <if test="counts != null">counts,</if>
            <if test="mobileStatus != null">mobileStatus,</if>
            <if test="totalCount != null">totalCount,</if>
        </trim>
    </sql>
    <sql id="sms_record_V">
        <trim suffix="" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="keyVal != null">#{keyVal},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="counts != null">#{counts},</if>
            <if test="mobileStatus != null">#{mobileStatus},</if>
            <if test="totalCount != null">#{totalCount},</if>
        </trim>
    </sql>
    <sql id="sms_record_U">
        <trim prefix="SET" suffixOverrides=",">
            <if test="keyVal != null">keyVal=#{keyVal},</if>
            <if test="createTime != null">createTime=#{createTime},</if>
            <if test="counts != null">counts=#{counts},</if>
            <if test="mobileStatus != null">mobileStatus=#{mobileStatus},</if>
            <if test="totalCount != null">totalCount=#{totalCount},</if>
        </trim>
    </sql>
    <sql id="sms_record_C">
        <trim prefix="WHERE" prefixOverrides="or | and">
            <if test="id != null">and id=#{id}</if>
            <if test="keyVal != null">and keyVal=#{keyVal}</if>
            <if test="createTime != null">and createTime=#{createTime}</if>
            <if test="counts != null">and counts=#{counts}</if>
            <if test="mobileStatus != null">and mobileStatus=#{mobileStatus},</if>
            <if test="totalCount != null">and totalCount=#{totalCount},</if>
        </trim>
    </sql>

    <insert id="save" parameterType="com.yg.pojo.SmsRecord" keyProperty="id"
            useGeneratedKeys="true">insert into
        t_sms_record (
        <include refid="sms_record_K"/>
        ) values(
        <include refid="sms_record_V"/>
        )
    </insert>

    <select id="queryById" parameterType="long" resultType="com.yg.pojo.SmsRecord">
        select * from t_sms_record where id=#{id}
    </select>

    <select id="queryAll" resultType="com.yg.pojo.SmsRecord">
        select * from t_sms_record
    </select>

    <select id="queryByPO" parameterType="com.yg.pojo.SmsRecord" resultType="com.yg.pojo.SmsRecord">
        select * from t_sms_record
        <include refid="sms_record_C"></include>
    </select>
    <select id="queryByPros" parameterType="map" resultType="com.yg.pojo.SmsRecord">
        select * FROM t_sms_record
        <trim prefix="where" prefixOverrides="or | and">
            <foreach collection="params" item="item" index="key" separator=" and ">
                <if test="null != item">${key}=#{item}</if>
            </foreach>
        </trim>
    </select>
    <select id="queryForPage" parameterType="com.yg.core.Page" resultType="com.yg.pojo.SmsRecord">
        select * from t_sms_record
        <trim prefix="where" prefixOverrides="or | and">
            <foreach collection="params" item="item" index="key" separator=" and ">
                <if test="null != item">
                    <choose>
                        <when test='key !="id"'>
                            ${key} LIKE '%${item}%'
                        </when>
                        <otherwise>
                            ${key}='${item}'
                        </otherwise>
                    </choose>
                </if>
            </foreach>
        </trim>
        <trim prefix="order by" prefixOverrides=",">
            <foreach collection="orderMap" item="item" index="key" separator=",">
                <if test="null != item">${key} ${item}</if>
            </foreach>
        </trim>
    </select>

    <update id="updateById" parameterType="com.yg.pojo.SmsRecord">
        update t_sms_record
        <include refid="sms_record_U"></include>
        where id=#{id}
    </update>
    <delete id="deleteById" parameterType="long">
        delete from t_sms_record where id=#{id}
    </delete>
    <delete id="deleteByPO" parameterType="com.yg.pojo.SmsRecord">
        delete from t_sms_record
        <include refid="sms_record_C"></include>
    </delete>
</mapper>